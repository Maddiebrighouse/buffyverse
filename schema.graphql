schema {
  query: Query
}

type Person {
  id: String!
  name: String!
  alias: String!
  species: String!
  imageUrl: String!
  age: Int!
  occupation: String!
}

type Query {
  people: [Person!]!
  person(id: String!): Person!
  peopleBySpecies(species: String!): [Person!]!
}
