overwrite: true
schema: "backend/schema.graphql"
documents:
  - "src/graphql/**/*.graphql"
generates:
  src/graphQL/graphql.tsx:
    plugins:
      - typescript
      - typescript-urql
      - typescript-operations
