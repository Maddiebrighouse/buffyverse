query GetPeople($species: String) {
  people(species: $species) {
    id
    name
    species
    alias
    imageUrl
  }
}

query GetPerson($id: String!) {
  person(id: $id) {
    id
    species
    name
    age
    alias
    imageUrl
    occupation
  }
}
